import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harfbiharf.game',
  appName: 'حرف بحرف',
  webDir: 'dist',
  android: { backgroundColor: '#0E1726' },
  ios: { backgroundColor: '#0E1726' },
};

export default config;
